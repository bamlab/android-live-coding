<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LiveCoding" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <item name="android:windowLightStatusBar" tools:targetApi="23">@bool/use_light_system_bars
        </item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">
            @bool/use_light_system_bars
        </item>

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/nav_bar_scrim</item>
    </style>

    <style name="Theme.LiveCoding.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.LiveCoding.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.LiveCoding.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>